export const metadata = {
    title: "resumer des payments d'un eleve",
  };
  
  const Layout = ({ children }: { children: React.ReactNode }) => {
    return <>{children}</>;
  };
  
  export default Layout;
  