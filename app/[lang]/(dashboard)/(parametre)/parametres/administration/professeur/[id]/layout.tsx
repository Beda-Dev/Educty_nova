export const metadata = {
    title: "Paramètres d'un Enseignant",
  };
  
  const Layout = ({ children }: { children: React.ReactNode }) => {
    return <>{children}</>;
  };
  
  export default Layout;
  